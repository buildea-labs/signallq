{
  "permissions": {
    "defaultMode": "bypassPermissions",
    "allow": [
      "Bash(gh pr merge *)"
    ]
  },
  "autoMode": {
    "allow": [
      "$defaults",
      "Bash(gh pr merge *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'echo \"$CLAUDE_TOOL_INPUT\" | python3 -c \"import json,sys; d=json.load(sys.stdin); cmd=d.get(\\\"command\\\",\\\"\\\"); exit(0 if any(p in cmd for p in [\\\"git push\\\",\\\"gh pr merge\\\"]) else 1)\" 2>/dev/null && bash \"${CLAUDE_PROJECT_DIR}/scripts/observe-and-act.sh\" || true'",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
